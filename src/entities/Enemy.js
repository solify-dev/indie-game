// Placeholder — full implementation in the next step.
export class Enemy {
  constructor(x, y, type = 'shambler') {
    this.x      = x;
    this.y      = y;
    this.type   = type;
    this.active = true;
  }
  update() {}
  draw()   {}
}
