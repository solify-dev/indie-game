// Placeholder — full implementation in the weapons step.
export class Projectile {
  constructor(x, y) {
    this.x      = x;
    this.y      = y;
    this.active = true;
  }
  update() {}
  draw()   {}
}
