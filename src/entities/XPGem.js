// Placeholder — full implementation in the XP/level-up step.
export class XPGem {
  constructor(x, y, value = 1) {
    this.x      = x;
    this.y      = y;
    this.value  = value;
    this.active = true;
  }
  update() {}
  draw()   {}
}
